- : pending
+ : done

+ use libelf for manual header extraction instead of objdump/objcopy hack
  (used GNU BFD)
+ cache DLL catalog
+ better cmdline handling in ld_ocaml: whether to show debug info, extra
  paths, etc. Maybe use environment vars instead? (used envvars in the end)
- Sys.argv rewriting


